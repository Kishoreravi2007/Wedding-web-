name: backend-deploy-trigger
description: Deploy Backend API on push to main (paths filter applied)
filename: cloudbuild-backend.yaml
repositoryEventConfig:
  repository: projects/sub-projects-483107/locations/asia-south1/connections/wedding-github-connection/repositories/wedding-web-repo
  push:
    branch: "^main$"
includedFiles:
  - "backend/**"
  - "cloudbuild-backend.yaml"
