name: deepface-deploy-trigger
description: Deploy DeepFace API on push to main (paths filter applied)
filename: cloudbuild-deepface.yaml
repositoryEventConfig:
  repository: projects/sub-projects-483107/locations/asia-south1/connections/wedding-github-connection/repositories/wedding-web-repo
  push:
    branch: "^main$"
includedFiles:
  - "backend/**"
  - "cloudbuild-deepface.yaml"
