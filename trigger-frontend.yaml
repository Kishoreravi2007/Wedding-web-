name: frontend-deploy-trigger
description: Deploy Frontend on push to main (paths filter applied)
filename: cloudbuild-frontend.yaml
repositoryEventConfig:
  repository: projects/sub-projects-483107/locations/asia-south1/connections/wedding-github-connection/repositories/wedding-web-repo
  push:
    branch: "^main$"
includedFiles:
  - "frontend/**"
  - "cloudbuild-frontend.yaml"
